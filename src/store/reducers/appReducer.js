import * as types from '../constants';

const initialState = {
	token: null,
	user: null
}

export default function appReducer(state = initialState, action = {}) {
	switch (action.type) {
		// case value:

		// 	break;

		default:
			return state;
	}
}